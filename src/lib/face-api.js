'use client';

// This file is no longer used, but kept to avoid breaking imports if it was referenced elsewhere.
// The face detection functionality has been removed to improve performance.

export async function loadModels() {
  // Models are no longer loaded.
  return;
} 