// Temporarily disabled due to import issues
export const GET = () => new Response('API not configured');
export const POST = () => new Response('API not configured');
export const OPTIONS = () => new Response('API not configured'); 